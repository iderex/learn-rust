<!-- Deutsch zuerst, dann Englisch. Was nicht zutrifft, wird gelöscht und nicht
     leer stehen gelassen. / German first, then English. Anything that does not
     apply gets deleted rather than left empty. -->

Schließt # / Closes #

## Was sich ändert / What changes

<!-- Deutsch: ein Absatz, was diese Änderung tut und welchen Fehler sie
     verhindert. / English: one paragraph on what this change does and what
     failure it prevents. -->

## Der Prüflauf / The check run

<!-- Deutsch: Die Befehle stehen in CONTRIBUTING.md unter
     "Die Befehle / The commands" und werden hier nicht abgeschrieben. Hier
     steht ihre Ausgabe. Ein Befehl, der nicht gelaufen ist, wird genannt,
     zusammen mit dem Grund. Ein leerer Abschnitt heißt nicht, dass alles grün
     war.
     English: the commands live in CONTRIBUTING.md under
     "Die Befehle / The commands" and are not copied here. What goes here is
     their output. A command that did not run is named together with the
     reason. An empty section does not mean everything was green. -->

```console

```

## Sprachen / Languages

<!-- Deutsch: Beide Sprachen da, oder nur eine? Bei nur einer bekommt dieser
     Pull Request text-de oder text-en und es gibt ein offenes
     Übersetzungs-Issue. / English: both languages present, or only one? With
     only one this pull request gets text-de or text-en and a translation issue
     stays open. -->

## Lizenz und Herkunft / Licence and origin

- [ ] Die Texte gehen unter CC BY 4.0, der Code unter MIT. / The texts go under CC BY 4.0, the code under MIT.
- [ ] Jeder Commit trägt die `Signed-off-by`-Zeile aus `git commit -s`. / Every commit carries the `Signed-off-by` line from `git commit -s`.
