//! Deutsch: Das Modul `texte`, ein zweiter Ast neben `zahlen`. Die beiden
//! wissen nichts voneinander.
//!
//! English: the module `texte`, a second branch beside `zahlen`. The two know
//! nothing of each other.

// Deutsch: Die Aufgabe ist offen, ihr Rumpf ist `todo!()`, und der Parameter
// bleibt deshalb ungenutzt, bis jemand sie löst.
// English: The exercise is open, its body is `todo!()`, and its parameter
// therefore stays unused until somebody solves it.
#![allow(unused_variables)]

/// Aufgabe 3: Gib den Text in Großbuchstaben mit einem Ausrufezeichen zurück.
///
/// Aus "hallo" wird "HALLO!". Gelöst wird das in `src/texte.rs`.
///
/// Exercise 3: return the text in capitals with an exclamation mark.
///
/// Out of "hallo" comes "HALLO!". This is solved in `src/texte.rs`.
pub fn shouted(text: &str) -> String {
    todo!("Aufgabe 3 / Exercise 3")
}
