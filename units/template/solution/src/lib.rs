//! <nn-nn> <Titel deutsch> / <title english>, gelöst.
//!
//! Deutsch: Die Erklärung steht in `units/<nn-nn-name>/README.md`. Hier stehen
//! nur die Rümpfe, die die Tests der Einheit grün machen.
//!
//! English: the explanation lives in `units/<nn-nn-name>/README.md`. What is
//! here is only the bodies that turn the unit's tests green.

/// <Was diese Beispielfunktion zeigt.>
///
/// <What this example function shows.>
pub fn example(n: u32) -> u32 {
    n * 2
}

/// <Was die Aufgabe verlangt.>
///
/// <What the exercise asks for.>
pub fn exercise(n: u32) -> u32 {
    n * 2
}
